import XCTest
@testable import TRSaldaView

final class TRSaldaViewTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TRSaldaView().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
